- 路由通过 `createRoute({ getParentRoute, path, component })` 声明式注册，并以命名导出（如 `downloadRoute`）供外部聚合。
- 状态映射统一用常量字典集中维护（`STATUS_COLORS`、`STATUS_LABELS`），避免在 JSX 中硬编码颜色或文案。
- 副作用（数据拉取、SSE 订阅、清理）集中在 `useEffect` 中，并通过 `useCallback` 包裹回调函数以保证依赖稳定。